Drop an approved 1200x630 OpenGraph hero image here as:

    og-thanxma-placeholder.jpg

It is referenced by /index.html in the og:image and twitter:image tags.

Until then, link previews on social platforms will fail to render an image.
